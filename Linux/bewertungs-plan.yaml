network:
  version: 2
  renderer: NetworkManager
  wifis:
    WLAN_DEVICE_PLACEHOLDER:
      dhcp4: yes
      dhcp6: yes
      access-points:
        "Bewertung":
          auth:
            key-management: "psk"

